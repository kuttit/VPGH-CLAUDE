version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: payment-rails-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: payment_rails
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5

  # Uncomment to run the API in Docker
  # api:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: payment-rails-api
  #   restart: unless-stopped
  #   environment:
  #     DATABASE_URL: postgresql://postgres:password@postgres:5432/payment_rails?schema=public
  #     NODE_ENV: development
  #     PORT: 3000
  #   ports:
  #     - '3000:3000'
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - ./src:/app/src
  #     - ./prisma:/app/prisma

volumes:
  postgres_data:
    driver: local
